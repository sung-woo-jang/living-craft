# HTTP → HTTPS 리다이렉트 (모든 서브도메인 공통)
server {
    listen 80;
    server_name holyseed.p-e.kr ad.holyseed.p-e.kr wedding.holyseed.p-e.kr lab.holyseed.p-e.kr;
    client_max_body_size 10M;
    # client_body_temp 디렉토리 소유자가 nobody라 실제 nginx worker 유저(jangseong-u)가 못 써서
    # 버퍼(기본 16k)를 넘는 요청 바디를 디스크에 못 내리고 500이 나던 문제 — 버퍼를 최대 바디 크기만큼
    # 늘려 항상 메모리에서 처리되게 함 (임시 디렉토리 소유권 자체를 고치는 게 근본 해결책이나 sudo 필요)
    client_body_buffer_size 10M;

    # Let's Encrypt 인증용 경로
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
        default_type "text/plain";
        allow all;
    }

    # 모든 HTTP 요청을 HTTPS로 리다이렉트
    location / {
        return 301 https://$host$request_uri;
    }
}

# holyseed.p-e.kr (루트: API 서버, 업로드 파일)
server {
    listen 443 ssl;
    http2 on;
    server_name holyseed.p-e.kr;
    client_max_body_size 10M;
    # client_body_temp 디렉토리 소유자가 nobody라 실제 nginx worker 유저(jangseong-u)가 못 써서
    # 버퍼(기본 16k)를 넘는 요청 바디를 디스크에 못 내리고 500이 나던 문제 — 버퍼를 최대 바디 크기만큼
    # 늘려 항상 메모리에서 처리되게 함 (임시 디렉토리 소유권 자체를 고치는 게 근본 해결책이나 sudo 필요)
    client_body_buffer_size 10M;

    ssl_certificate /etc/letsencrypt/live/holyseed.p-e.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/holyseed.p-e.kr/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    access_log /usr/local/var/log/nginx/holyseed-access.log;
    error_log /usr/local/var/log/nginx/holyseed-error.log;

    # Health Check (prefix 없음)
    location /health {
        proxy_pass http://127.0.0.1:8000/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # API 리버스 프록시
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 업로드 파일 서빙
    location /uploads {
        alias /Users/jangseong-u/production/uploads;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 루트: 아직 서빙할 서비스 없음
    location / {
        return 404;
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# ad.holyseed.p-e.kr (자산일기 웹앱)
server {
    listen 443 ssl;
    http2 on;
    server_name ad.holyseed.p-e.kr;
    client_max_body_size 10M;
    # client_body_temp 디렉토리 소유자가 nobody라 실제 nginx worker 유저(jangseong-u)가 못 써서
    # 버퍼(기본 16k)를 넘는 요청 바디를 디스크에 못 내리고 500이 나던 문제 — 버퍼를 최대 바디 크기만큼
    # 늘려 항상 메모리에서 처리되게 함 (임시 디렉토리 소유권 자체를 고치는 게 근본 해결책이나 sudo 필요)
    client_body_buffer_size 10M;

    ssl_certificate /etc/letsencrypt/live/holyseed.p-e.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/holyseed.p-e.kr/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    access_log /usr/local/var/log/nginx/ad-access.log;
    error_log /usr/local/var/log/nginx/ad-error.log;

    # API 리버스 프록시 (백엔드는 holyseed.p-e.kr:8000 로 통일)
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # ad-native(RN 앱) OTA 업데이트 번들 — SPA fallback(location /)보다 먼저 매칭돼야 함.
    # 각 업데이트가 UUID 경로로 유니크하게 저장되므로 영구 캐시해도 안전 (/uploads와 동일 패턴)
    location /ad-native-updates {
        alias /Users/jangseong-u/ad-native-updates;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 자산일기 SPA
    location / {
        root /var/www/ad-front;
        index index.html;
        try_files $uri $uri/ /index.html;

        # PWA 서비스워커/매니페스트 — 아래 정적자산 immutable 캐시보다 우선(정확매치가 정규식보다 우선순위 높음).
        # 장기 캐시되면 브라우저가 새 버전을 못 받아가 서비스워커가 갱신되지 않는다.
        location = /sw.js {
            add_header Cache-Control "no-cache";
        }
        location = /manifest.webmanifest {
            add_header Cache-Control "no-cache";
        }

        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# wedding.holyseed.p-e.kr (결혼식 아카이브 웹앱)
server {
    listen 443 ssl;
    http2 on;
    server_name wedding.holyseed.p-e.kr;
    client_max_body_size 10M;
    # client_body_temp 디렉토리 소유자가 nobody라 실제 nginx worker 유저(jangseong-u)가 못 써서
    # 버퍼(기본 16k)를 넘는 요청 바디를 디스크에 못 내리고 500이 나던 문제 — 버퍼를 최대 바디 크기만큼
    # 늘려 항상 메모리에서 처리되게 함 (임시 디렉토리 소유권 자체를 고치는 게 근본 해결책이나 sudo 필요)
    client_body_buffer_size 10M;

    ssl_certificate /etc/letsencrypt/live/holyseed.p-e.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/holyseed.p-e.kr/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    access_log /usr/local/var/log/nginx/wedding-access.log;
    error_log /usr/local/var/log/nginx/wedding-error.log;

    # API 리버스 프록시 (백엔드는 holyseed.p-e.kr:8000 로 통일)
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # 청첩장 슬러그 경로(예: /kim-n-jang) 전용 — 카카오톡 등 링크 공유 미리보기 크롤러는
    # JS를 실행하지 않아 SPA가 나중에 채우는 OG 메타태그를 못 읽으므로, 해당 크롤러 요청만
    # 백엔드가 렌더링한 정적 OG HTML로 응답(/api/wedding/og/:slug). 그 외 방문자는 SPA 그대로.
    # (if를 location / 안의 중첩 location과 함께 쓰면 try_files가 깨지는 알려진 문제가 있어
    #  분리된 location에서만 처리 — https://www.nginx.com/resources/wiki/start/topics/depth/ifisevil/)
    location ~* ^/(?!admin|login|register)([a-zA-Z0-9_-]+)/?$ {
        set $invite_slug $1;
        # 카카오톡 인앱 브라우저(사람이 실제로 접속) UA는 "KAKAOTALK"만 붙고, 링크 미리보기
        # 크롤러 UA는 "kakaotalk-scrap"이 붙음 — 여기서는 크롤러만 걸러야 하므로 "kakaotalk-scrap"으로
        # 좁혀야 함. "kakaotalk"으로 넓게 잡으면 인앱 브라우저 실사용자까지 이 텅 빈 OG 페이지를
        # 받아서 "카카오톡에서 청첩장이 안 열린다"는 문제로 이어짐
        if ($http_user_agent ~* "kakaotalk-scrap|facebookexternalhit|twitterbot|slackbot|telegrambot|discordbot") {
            rewrite ^ /api/wedding/og/$invite_slug last;
        }

        root /var/www/wedding-front;
        try_files /index.html =404;
    }

    # 결혼식 아카이브 SPA
    location / {
        root /var/www/wedding-front;
        index index.html;
        try_files $uri $uri/ /index.html;

        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# lab.holyseed.p-e.kr (개인 다목적 대시보드)
server {
    listen 443 ssl;
    http2 on;
    server_name lab.holyseed.p-e.kr;
    client_max_body_size 10M;
    # client_body_temp 디렉토리 소유자가 nobody라 실제 nginx worker 유저(jangseong-u)가 못 써서
    # 버퍼(기본 16k)를 넘는 요청 바디를 디스크에 못 내리고 500이 나던 문제 — 버퍼를 최대 바디 크기만큼
    # 늘려 항상 메모리에서 처리되게 함 (임시 디렉토리 소유권 자체를 고치는 게 근본 해결책이나 sudo 필요)
    client_body_buffer_size 10M;

    ssl_certificate /etc/letsencrypt/live/holyseed.p-e.kr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/holyseed.p-e.kr/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

    access_log /usr/local/var/log/nginx/lab-access.log;
    error_log /usr/local/var/log/nginx/lab-error.log;

    # vite preview가 상시 서빙 (pm2 lab-front, /api → :8000 프록시 내장)
    location / {
        proxy_pass http://127.0.0.1:4800;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
